import {ReactComponent as ReactLogo} from '../check-mark-circle-2-svgrepo-com.svg';

const TickIcon = () => {
  return (
    <div>
      <ReactLogo className="tick"/>
    </div>
  );
};

export default TickIcon;
